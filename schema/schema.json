{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/robgonnella/minienv/schema/schema.json",
  "$defs": {
    "ChartImage": {
      "properties": {
        "repository": {
          "type": "string",
          "description": "Image Repository for the service image"
        },
        "tag": {
          "type": "string",
          "description": "Image tag for the service image"
        },
        "platforms": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "The platforms for which to build and push default [linux/amd64])"
        },
        "pullPolicy": {
          "type": "string",
          "description": "PullPolicy for this image"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "repository",
        "tag"
      ],
      "description": "ChartImage is the configuration for a service image."
    },
    "ChartImagePullSecret": {
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the secret for pulling images"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name"
      ],
      "description": "ChartImagePullSecret names a secret that enables pulling private images."
    },
    "ChartService": {
      "properties": {
        "create": {
          "type": "boolean",
          "description": "Whether or not to create a Kubernetes service"
        },
        "type": {
          "type": "string",
          "description": "The type of service to create"
        },
        "ports": {
          "items": {
            "$ref": "#/$defs/ChartServicePort"
          },
          "type": "array",
          "description": "The ports to associate with pod container and service mapping"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ports"
      ],
      "description": "ChartService is the Kubernetes service configuration."
    },
    "ChartServiceAccount": {
      "properties": {
        "create": {
          "type": "boolean",
          "description": "Whether or not to create a Kubernetes service account"
        },
        "name": {
          "type": "string",
          "description": "The name for the service account"
        },
        "automount": {
          "type": "boolean",
          "description": "Whether or not to automount the service account"
        },
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Additional annotations for the service account"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "ChartServiceAccount is the service account configuration for the deployment."
    },
    "ChartServicePort": {
      "properties": {
        "containerPortName": {
          "type": "string",
          "description": "Name of the port, used by both the service and the container"
        },
        "containerPort": {
          "type": "integer",
          "description": "Container port to expose, which the service exposes under the same number"
        },
        "protocol": {
          "type": "string",
          "description": "Protocol to use for these ports"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "containerPortName",
        "containerPort",
        "protocol"
      ],
      "description": "ChartServicePort is the port configuration used by both the service and the deployment pod container."
    },
    "NgrokServiceLevel": {
      "properties": {
        "port": {
          "type": "integer",
          "description": "Exposes the specified container port publicly via ngrok. This port must\nmatch one of the container ports resolved for the service, which is the\ncontainer side of a docker-compose port mapping."
        },
        "url": {
          "type": "string",
          "description": "Ngrok url configuration for the service endpoint"
        },
        "trafficPolicy": {
          "type": "string",
          "description": "Ngrok \"on_http_request\" configuration"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "port"
      ],
      "description": "NgrokServiceLevel is the configuration for exposing a service publicly."
    },
    "NgrokTopLevel": {
      "properties": {
        "trafficPolicy": {
          "type": "string",
          "description": "Ngrok \"on_http_request\" configuration"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "NgrokTopLevel is the ngrok configuration applied to all services unless overridden per service."
    },
    "ServiceImage": {
      "properties": {
        "repository": {
          "type": "string",
          "description": "Image Repository for the service image"
        },
        "tag": {
          "type": "string",
          "description": "Image tag for the service image"
        },
        "platforms": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "The platforms for which to build and push default [linux/amd64])"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "repository",
        "tag"
      ],
      "description": "ServiceImage represents the image that will be pulled and used for a service in the remote environment."
    },
    "XMiniEnv": {
      "properties": {
        "k8s": {
          "$ref": "#/$defs/XMiniEnvK8s",
          "description": "K8s configuration for deploying to Kubernetes"
        },
        "docker": {
          "$ref": "#/$defs/XMiniEnvDocker",
          "description": "Docker uses docker and a specified transport mechanism to deploy modified\ndocker-compose config on a remote server"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "XMiniEnv top-level extension configuration for deploying to various targets."
    },
    "XMiniEnvDocker": {
      "properties": {
        "namespace": {
          "type": "string",
          "description": "The namespace for this deployment to ensure there are no conflicts\nwith other user minienvs on the same host"
        },
        "transport": {
          "$ref": "#/$defs/XMiniEnvDockerTransport",
          "description": "The transport mechanism to use for communication with the remote server"
        },
        "ngrok": {
          "$ref": "#/$defs/NgrokTopLevel",
          "description": "Ngrok configuration for exposing services publicly"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "namespace",
        "transport"
      ],
      "description": "XMiniEnvDocker uses docker and a specified transport mechanism to deploy modified docker-compose config on a remote server."
    },
    "XMiniEnvDockerCopy": {
      "properties": {
        "hostPath": {
          "type": "string",
          "description": "The path, relative to the project directory, of the file or directory to be copied"
        },
        "containerPath": {
          "type": "string",
          "description": "The absolute path inside the container to mount the file or directory"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "hostPath",
        "containerPath"
      ],
      "description": "XMiniEnvDockerCopy copies a file or directory to the remote server and bind mounts it into the container at the specified containerPath."
    },
    "XMiniEnvDockerService": {
      "properties": {
        "ngrok": {
          "$ref": "#/$defs/NgrokServiceLevel",
          "description": "Ngrok configuration for exposing services publicly"
        },
        "skip": {
          "type": "boolean",
          "description": "Prevents the targeted service from being deployed to the cluster"
        },
        "image": {
          "$ref": "#/$defs/ServiceImage",
          "description": "The image for this deployment. Will try to use compose service image if not set"
        },
        "copy": {
          "items": {
            "$ref": "#/$defs/XMiniEnvDockerCopy"
          },
          "type": "array",
          "description": "Files and directories copied to the remote server and bind mounted into\nthe container. Normal compose bind mounts are intentionally discarded\nmaking this config the override escape hatch when needed."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "XMiniEnvDockerService is the service level configuration controlling docker deployment properties."
    },
    "XMiniEnvDockerTransport": {
      "properties": {
        "ssh": {
          "$ref": "#/$defs/XMiniEnvSSH",
          "description": "Copies files and runs commands on the remote server over SSH"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "XMiniEnvDockerTransport holds the transport mechanism for copying files and running commands on a remote server."
    },
    "XMiniEnvK8s": {
      "properties": {
        "context": {
          "type": "string",
          "description": "Targets a specific cluster when deploying"
        },
        "namespace": {
          "type": "string",
          "description": "Targets a specific namespace when deploying"
        },
        "deploymentTimeout": {
          "type": "string",
          "description": "Controls the Helm timeout. This is applied to all services but can be\noverridden using the service-level extension"
        },
        "ngrok": {
          "$ref": "#/$defs/NgrokTopLevel",
          "description": "Ngrok configuration for exposing services publicly"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "context",
        "namespace"
      ],
      "description": "XMiniEnvK8s holds the fields required for deploying to Kubernetes."
    },
    "XMiniEnvK8sService": {
      "properties": {
        "ngrok": {
          "$ref": "#/$defs/NgrokServiceLevel",
          "description": "Ngrok configuration for exposing services publicly"
        },
        "skip": {
          "type": "boolean",
          "description": "Prevents the targeted service from being deployed to the cluster"
        },
        "replicas": {
          "type": "integer",
          "description": "The number of replicas for this deployment"
        },
        "image": {
          "$ref": "#/$defs/ChartImage",
          "description": "The image for this deployment. Will try to use compose service image if not set"
        },
        "imagePullSecrets": {
          "items": {
            "$ref": "#/$defs/ChartImagePullSecret"
          },
          "type": "array",
          "description": "Any image pull secrets required to pull images on the cluster"
        },
        "command": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Command to run for the main deployment container"
        },
        "service": {
          "$ref": "#/$defs/ChartService",
          "description": "Service configuration including container and service port specifications"
        },
        "serviceAccount": {
          "$ref": "#/$defs/ChartServiceAccount",
          "description": "Service account configuration"
        },
        "env": {
          "items": {
            "type": "object"
          },
          "type": "array",
          "description": "Container environment variables as Kubernetes EnvVar entries, merged over compose environment by name"
        },
        "envFrom": {
          "items": {
            "type": "object"
          },
          "type": "array",
          "description": "Sources to populate container environment variables from, as Kubernetes EnvFromSource entries"
        },
        "podAnnotations": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Annotations to add to the deployment pods"
        },
        "podLabels": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object",
          "description": "Labels to add to the deployment pods"
        },
        "podSecurityContext": {
          "type": "object",
          "description": "Security context for the deployment pods"
        },
        "securityContext": {
          "type": "object",
          "description": "Security context for the container in each pod"
        },
        "resources": {
          "type": "object",
          "description": "Resources configuration for deployment pods"
        },
        "startupProbe": {
          "type": "object",
          "description": "Container startup probe configuration"
        },
        "livenessProbe": {
          "type": "object",
          "description": "Container liveness probe configuration"
        },
        "readinessProbe": {
          "type": "object",
          "description": "Container readiness probe configuration"
        },
        "volumes": {
          "items": {
            "type": "object"
          },
          "type": "array",
          "description": "Volumes configuration for the deployment pods"
        },
        "volumeMounts": {
          "items": {
            "type": "object"
          },
          "type": "array",
          "description": "VolumeMounts configuration for the container"
        },
        "nodeSelector": {
          "type": "object",
          "description": "NodeSelector configuration for the deployment pods"
        },
        "tolerations": {
          "items": {
            "type": "object"
          },
          "type": "array",
          "description": "Tolerations configuration for the deployment pods"
        },
        "affinity": {
          "type": "object",
          "description": "Affinity configuration for the deployment pods"
        },
        "recreate": {
          "type": "boolean",
          "description": "Recreates the service on each deploy even if values have not changed"
        },
        "deploymentType": {
          "type": "string",
          "enum": [
            "service",
            "job"
          ],
          "description": "Controls the type of deployment (service | job). Default is \"service\"",
          "default": "service"
        },
        "deploymentTimeout": {
          "type": "string",
          "description": "Controls the Helm timeout for deploying the targeted service"
        },
        "manifests": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Paths to Kubernetes manifest files, relative to the project directory, rendered as part of this service's chart"
        },
        "configMapFrom": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Paths to files, relative to the project directory, whose contents become this service's ConfigMap keyed by file name"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "XMiniEnvK8sService is the service level configuration controlling Kubernetes deployment properties."
    },
    "XMiniEnvSSH": {
      "properties": {
        "host": {
          "type": "string",
          "description": "The target host for the SSH connection"
        },
        "user": {
          "type": "string",
          "description": "The user for the SSH connection"
        },
        "port": {
          "type": "integer",
          "description": "The port for the SSH connection"
        },
        "identity": {
          "type": "string",
          "description": "The RSA private key identity file for the SSH connection"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "host",
        "user",
        "port",
        "identity"
      ],
      "description": "XMiniEnvSSH holds the fields required for deploying to any SSH-enabled remote server."
    },
    "service": {
      "allOf": [
        {
          "$ref": "https://raw.githubusercontent.com/compose-spec/compose-go/master/schema/compose-spec.json#/$defs/service",
          "description": "The official docker-compose service spec"
        }
      ],
      "properties": {
        "x-minienv-k8s-service": {
          "$ref": "#/$defs/XMiniEnvK8sService"
        },
        "x-minienv-docker-service": {
          "$ref": "#/$defs/XMiniEnvDockerService"
        }
      }
    }
  },
  "allOf": [
    {
      "$ref": "https://raw.githubusercontent.com/compose-spec/compose-go/master/schema/compose-spec.json",
      "description": "The official docker-compose spec"
    }
  ],
  "properties": {
    "x-minienv": {
      "$ref": "#/$defs/XMiniEnv"
    },
    "services": {
      "patternProperties": {
        "^[a-zA-Z0-9._-]+$": {
          "$ref": "#/$defs/service"
        }
      },
      "description": "Wrapped docker-compose service spec with minienv extensions"
    }
  },
  "description": "Minienv schema wrapping official docker-compose schema but with minienv extensions"
}